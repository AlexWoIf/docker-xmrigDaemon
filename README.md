# docker-xmrigDaemon
```
git clone https://github.com/AlexWoIf/docker-xmrigDaemon
cd docker-xmrigDaemon
sudo docker run -d --name xmrigcc-daemon -v $(pwd)/config.json:/xmrigCC/config.json bendr0id/xmrigcc
```